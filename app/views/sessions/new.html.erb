<h1>登入</h1>

<%= form_for(@user, url: sign_in_sessions_path) do |f| %>   
  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email %>
  </div>

  <div class="field">
    <%= f.label :pasword %>
    <%= f.password_field :password %>
  </div>

  <%= f.submit '登入' %>
<% end %>